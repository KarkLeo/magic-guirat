# AUDIO-1: Захват звука с микрофона

**Дата:** 2026-02-06
**Статус:** ✅ Завершено
**Sprint:** Sprint 1 MVP
**User Story:** US-1.1

---

## Задача

Реализовать захват звука с микрофона для дальнейшего анализа игры на гитаре.

### Критерии приемки US-1.1:
- [x] Запрос доступа к микрофону
- [x] Реализован аудио стрим в реальном времени
- [x] Обработка ошибок доступа к микрофону
- [x] Индикатор уровня входного сигнала

---

## Реализация

### 1. Composable: `src/composables/useAudioCapture.js`

**Функционал:**
- Захват звука через `navigator.mediaDevices.getUserMedia()`
- Создание `AudioContext` и `AnalyserNode`
- Настройки для гитары (отключение эха, шумоподавления, автоусиления)
- Мониторинг уровня сигнала через RMS (Root Mean Square)
- Обработка ошибок: NotAllowed, NotFound, NotReadable
- Автоматическая очистка ресурсов при размонтировании

**API:**
```javascript
const {
  isCapturing,          // Статус захвата
  isRequestingPermission, // Запрос permission
  error,                // Ошибка (если есть)
  audioLevel,           // Уровень сигнала (0-1)
  hasError,             // Есть ли ошибка
  canCapture,           // Можно ли начать захват
  startCapture,         // Запустить захват
  stopCapture,          // Остановить захват
  getAnalyserNode,      // Получить AnalyserNode
  getAudioContext       // Получить AudioContext
} = useAudioCapture()
```

**Технические детали:**
```javascript
// AudioContext настройки
analyserNode.fftSize = 2048
analyserNode.smoothingTimeConstant = 0.8

// getUserMedia настройки
{
  audio: {
    echoCancellation: false,
    noiseSuppression: false,
    autoGainControl: false
  }
}
```

### 2. UI Компонент: `src/components/AudioCaptureButton.vue`

**Функционал:**
- Кнопка запуска/остановки захвата
- Иконки микрофона (обычная/пауза/спиннер)
- Визуальный индикатор уровня сигнала (Volume Meter)
- Отображение ошибок пользователю
- Анимации и переходы

**Визуализация:**
- Фиолетово-синяя магическая палитра
- Градиенты: #667eea → #764ba2
- Активное состояние: #f093fb → #f5576c с pulse анимацией
- Индикатор уровня с градиентом и свечением

### 3. Интеграция: `src/App.vue`

**Изменения:**
- Заменён VisualizationComparison на AudioCaptureButton
- Добавлен темный градиентный фон (#0f0c29 → #302b63 → #24243e)
- Заголовок "🎸 Magic Guitar" с магическим эффектом
- Футер с информацией о спринте

---

## Технические решения

### Web Audio API Architecture

```
MediaStream (микрофон)
  ↓
MediaStreamSource
  ↓
AnalyserNode (FFT 2048)
  ↓
getByteTimeDomainData()
  ↓
RMS Calculation
  ↓
audioLevel (0-1)
```

### Алгоритм RMS для уровня сигнала

```javascript
// Получаем waveform данные
analyserNode.getByteTimeDomainData(dataArray)

// Вычисляем RMS
let sum = 0
for (let i = 0; i < dataArray.length; i++) {
  const normalized = (dataArray[i] - 128) / 128
  sum += normalized * normalized
}
const rms = Math.sqrt(sum / dataArray.length)
const audioLevel = Math.min(rms * 2, 1)
```

### Обработка ошибок

| Тип ошибки | Сообщение |
|------------|-----------|
| `NotAllowedError` | Доступ к микрофону запрещён. Разрешите доступ в настройках браузера. |
| `NotFoundError` | Микрофон не найден. Подключите микрофон и попробуйте снова. |
| `NotReadableError` | Микрофон используется другим приложением. |
| Другие | Ошибка при доступе к микрофону: {message} |

---

## Дизайн и стилизация

### Цветовая палитра

**Градиенты кнопок:**
- Неактивная: `#667eea → #764ba2`
- Активная: `#f093fb → #f5576c`
- Запрос: `#a8b5ff → #c5b4e3`
- Ошибка: `#ff6b6b → #ee5a6f`

**Фон приложения:**
- `#0f0c29 → #302b63 → #24243e`

**Акценты:**
- Текст: `#a8b5ff`
- Заголовок: gradient `#667eea → #f093fb`

### Анимации

- **Pulse:** активная кнопка пульсирует (box-shadow)
- **Spinner:** вращение при запросе permission
- **Hover:** lift-up эффект при наведении
- **Transitions:** плавные переходы 0.3s

---

## Результаты тестирования

### Чек-лист функционала:
- ✅ Кнопка "Начать захват звука" работает
- ✅ Браузер корректно запрашивает permission
- ✅ После разрешения захват стартует
- ✅ Индикатор уровня сигнала отображается
- ✅ Индикатор реагирует на звук в реальном времени
- ✅ Кнопка "Остановить захват" работает
- ✅ Обработка отказа в доступе корректна
- ✅ Визуализация соответствует магической теме

### Производительность:
- AudioLevel обновляется через `requestAnimationFrame` (~60 FPS)
- Нет утечек памяти (ресурсы очищаются при stopCapture)
- Минимальная нагрузка на CPU

### Браузерная совместимость:
- ✅ Chrome/Edge (Web Audio API full support)
- ✅ Firefox (Web Audio API full support)
- ✅ Safari (Web Audio API with webkit prefix)

---

## Заметки и наблюдения

### Почему отключены audio effects?

```javascript
echoCancellation: false,
noiseSuppression: false,
autoGainControl: false
```

**Причина:** Эти фильтры могут искажать звук гитары:
- Echo cancellation удаляет отражения, но может срезать натуральный резонанс
- Noise suppression может удалять высокие частоты обертонов
- Auto gain control меняет уровень, что мешает динамике игры

Для точного pitch detection нужен **чистый сигнал**.

### FFT Size: 2048

**Выбор 2048:**
- Хорошее частотное разрешение: `sampleRate / fftSize / 2`
- Для 48kHz: `48000 / 2048 / 2 = 11.7 Hz` на bin
- Достаточно для различения нот гитары (минимум 5-6 Hz разница)
- Не слишком большой для real-time (latency ~43ms)

**Альтернативы:**
- 1024: быстрее, но хуже разрешение
- 4096: лучше разрешение, но больше latency

### Composable Pattern

**Преимущества:**
- Переиспользуемая логика (можно использовать в других компонентах)
- Разделение concerns (UI и audio logic отдельно)
- Testable (можно тестировать логику без UI)
- Vue 3 best practice

---

## Следующие шаги

### Готово для следующих задач:

**AUDIO-2: Частотный анализ FFT**
- AnalyserNode уже настроен и готов
- Можно получить через `getAnalyserNode()`
- FFT size 2048 оптимален для pitch detection

**AUDIO-3: Определение питча**
- AudioContext доступен
- Можно интегрировать Essentia.js
- Диапазон гитары: 82-1200 Hz (E2-E6)

**VIS-1: Визуализация струн**
- audioLevel можно использовать для анимации
- Структура UI готова для добавления визуализации
- Three.js интеграция следующая

---

## Файлы

**Созданные:**
- `src/composables/useAudioCapture.js` (182 строки)
- `src/components/AudioCaptureButton.vue` (299 строк)

**Изменённые:**
- `src/App.vue` (добавлена темная тема, интеграция компонента)

**Dev Server:**
- http://localhost:5174/
- Hot reload работает
- Vue DevTools доступен

---

## Метаданные

- **Задача:** AUDIO-1
- **User Story:** US-1.1
- **Sprint:** Sprint 1 MVP
- **Приоритет:** P0 (Must Have)
- **Статус:** ✅ Done
- **Дата:** 2026-02-06
- **Время:** ~1.5 часа
- **Sprint Progress:** 1/5 задач Sprint 1

---

*Archived by Claude Code*
